apiVersion: v1
kind: Service
metadata:
  name: itomata-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: itomata-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
