version: 0.2

env:
  variables:
    AWS_STS_REGIONAL_ENDPOINTS: "regional"

phases:
  pre_build:
    commands:
      - echo "Logging into Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - echo "Building Docker images..."
      - docker build -t itomata-frontend frontend/
      - docker build -t itomata-backend backend/

  post_build:
    commands:
      - echo "Pushing images to ECR..."
      - docker tag itomata-frontend:latest $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/itomata-frontend:latest
      - docker tag itomata-backend:latest $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/itomata-backend:latest
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/itomata-frontend:latest
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/itomata-backend:latest

      - chmod +x deploy.sh

      # --- REGION 1: MUMBAI ---
      - echo "🚀 Deploying to Mumbai (ap-south-1)..."
      - export AWS_DEFAULT_REGION=ap-south-1
      - aws eks update-kubeconfig --region ap-south-1 --name itomata-eks-cluster
      # We pass the token explicitly to the script environment
      - export KUBERNETES_TOKEN=$(aws eks get-token --cluster-name itomata-eks-cluster --region ap-south-1 | jq -r '.status.token')
      - ./deploy.sh

      # --- REGION 2: VIRGINIA ---
      - echo "🚀 Deploying to Virginia (us-east-1)..."
      - export AWS_DEFAULT_REGION=us-east-1
      - aws eks update-kubeconfig --region us-east-1 --name itomata-eks-cluster
      - export KUBERNETES_TOKEN=$(aws eks get-token --cluster-name itomata-eks-cluster --region us-east-1 | jq -r '.status.token')
      - ./deploy.sh

      - echo "✅ Multi-Region Blue/Green Deployment complete!"
